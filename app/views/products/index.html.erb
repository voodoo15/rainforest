<h1>Products</h1>

<% @products.each do | p | %>
<p>
  <%= link_to p.name, "/products/#{ p.id }" %>
  <%= p.description %>
  <%= p.formatted_price %>
</p>

<% end %>

<%= link_to "New Product", "/products/new" %>
